# Of course I found problems with 24_b...
main
var a, b;
{
	let a <- 1;
	let b <- 2;
	
	while (a / 4 + 5) < 8 do
		let a <- a * 7 + 9 / b;
		if 3 < 4 then
			let b <- ( 7 - 5 * a ) * b;
			while a > b do
				let a <- a + 1 + b
			od;
			let b <- b * b / a + 3
		else
			if 1 >= a then
				let b <- a * a * 7 - 2;
				while (b - 7) != a do
					let a <- a + a - 1;
					let b <- a - b + 1
				od;
				let a <- b * a * a
			else
				let b <- 725;
				while (a - 8) != 10 do
					let a <- a - 1;
					let b <- b + 4;
					let a <- b + 1
				od;
				let b <- (1 * 4) / 4
			fi;
			let b <- 1 + 10
		fi

	od;
	let b <- a * b;
	let a <- b + a - 7
}
.